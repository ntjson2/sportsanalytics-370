mixin thumbIndexMixin(apSport)
    .col-md-4  
        .card.mb-4.shadow-sm
            .card-body               
                .card-text #{apSport.id}
                .card-text #{apSport.teams.home.name}
                .card-text #{apSport.teams.visitors.name}